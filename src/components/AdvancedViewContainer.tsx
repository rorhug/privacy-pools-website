'use client';

import { styled } from '@mui/material';

export const AdvancedViewContainer = styled('div')(() => {
  return {
    marginTop: '2rem',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: '100%',
    height: '100%',
    gap: '2.4rem',
  };
});
